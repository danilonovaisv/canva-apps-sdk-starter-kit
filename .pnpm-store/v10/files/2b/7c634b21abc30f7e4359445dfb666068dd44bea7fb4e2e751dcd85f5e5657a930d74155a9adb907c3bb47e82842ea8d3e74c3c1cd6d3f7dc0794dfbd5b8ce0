import { jsx as _jsx } from "react/jsx-runtime";
import 'react';
import { Button } from '../../button/button';
import { ReloadIcon } from '../../icons/reload/icon';
import { XIcon } from '../../icons/x/icon';
import { ProgressBarMessages } from './progress_bar.messages';
export const ProgressCancelButton = (props)=>{
    const { onClick, ariaLabel, ...rest } = props;
    return _jsx(Button, {
        ...rest,
        variant: "tertiary",
        icon: XIcon,
        ariaLabel: ariaLabel ?? ProgressBarMessages.cancelButtonLabel(),
        onClick: onClick
    });
};
export const ProgressRetryButton = (props)=>{
    const { onClick, ariaLabel, ...rest } = props;
    return _jsx(Button, {
        ...rest,
        variant: "tertiary",
        icon: ReloadIcon,
        ariaLabel: ariaLabel ?? ProgressBarMessages.retryButtonLabel(),
        onClick: onClick
    });
};
