"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get TRANSITION_END_TIMEOUT_MS () {
        return TRANSITION_END_TIMEOUT_MS;
    },
    get useProgressAnimationEnd () {
        return useProgressAnimationEnd;
    }
});
const _react = _interop_require_wildcard(require("react"));
const _progress_bar_indicatorcss = _interop_require_default(require("./progress_bar_indicator.css"));
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
function useProgressAnimationEnd({ value, onProgressAnimationEnd }) {
    const handledRef = _react.useRef(false);
    const timeoutRef = _react.useRef(0);
    const onTransitionEnd = _react.useCallback(()=>{
        window.clearTimeout(timeoutRef.current);
        if (!handledRef.current) {
            handledRef.current = true;
            onProgressAnimationEnd?.(value);
        }
    }, [
        onProgressAnimationEnd,
        value
    ]);
    _react.useEffect(()=>{
        handledRef.current = false;
        window.clearTimeout(timeoutRef.current);
        timeoutRef.current = window.setTimeout(onTransitionEnd, TRANSITION_END_TIMEOUT_MS);
        return ()=>window.clearTimeout(timeoutRef.current);
    }, [
        value,
        onTransitionEnd
    ]);
    return {
        onTransitionEnd
    };
}
const TRANSITION_END_TIMEOUT_MS = parseInt(_progress_bar_indicatorcss.default.widthTransitionDurationMs, 10) + 100 || 2000;
