type UnionToIntersection<U> = (U extends unknown ? (x: U) => void : never) extends (x: infer I) => void ? I : never;
export declare function composeHandles<Handles extends object[]>(...handles: Handles): UnionToIntersection<Handles[number]>;
export {};
