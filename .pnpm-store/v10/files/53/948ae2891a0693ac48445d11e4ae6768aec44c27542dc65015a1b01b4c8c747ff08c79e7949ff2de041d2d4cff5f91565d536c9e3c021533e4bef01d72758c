"use strict"
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "useDateTimePickerState", {
    enumerable: true,
    get: function() {
        return useDateTimePickerState;
    }
});
const _stable_function = require('../../../../../base/react/stable_function');
const _use_isomorphic_layout_effect = require('../../../../../base/react/use_isomorphic_layout_effect');
const _react = _interop_require_wildcard(require("react"));
const _utils = require('../../utils/utils');
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
function toUtcDateString(dateInTimezone, dayTime, defaultTimezone) {
    const localDate = (0, _utils.setTimeToDate)(dateInTimezone, dayTime);
    const timezoneOffset = defaultTimezone ? defaultTimezone.offset : (0, _utils.getTimezoneOffset)(localDate);
    let utcDate = (0, _utils.fromTimezoneToUTC)(localDate, timezoneOffset);
    const utcDateTimezoneOffset = defaultTimezone ? defaultTimezone.offset : (0, _utils.getTimezoneOffset)(utcDate);
    if (timezoneOffset !== utcDateTimezoneOffset)
        utcDate = (0, _utils.fromTimezoneToUTC)(localDate, utcDateTimezoneOffset);
    return (0, _utils.toISO)(utcDate);
}
function useDateTimePickerState({ date, locale, today: todayProp, defaultTimezone, focusToday }) {
    const [today] = _react.useState(()=>todayProp || (0, _utils.toISO)(new Date()));
    const [initialFocusDate] = _react.useState(()=>{
        if (!focusToday) return undefined;
        const initialViewDate = date ?? today;
        const todayTzOffset = defaultTimezone ? defaultTimezone.offset : (0, _utils.getTimezoneOffset)(today);
        const todayInTz = (0, _utils.toUTCDateTimeObject)((0, _utils.fromUTCToTimezone)(today, todayTzOffset));
        const initialTz = defaultTimezone || (0, _utils.getTimezone)(initialViewDate, locale);
        const initialDateInTz = (0, _utils.toUTCDateTimeObject)((0, _utils.fromUTCToTimezone)(initialViewDate, initialTz.offset));
        return toUtcDateString(todayInTz, (0, _utils.getTimeOfDay)(initialDateInTz), defaultTimezone);
    });
    const [viewDate, setViewDate] = _react.useState(()=>initialFocusDate ?? date ?? today);
    const [focusedDate, setFocusedDate] = _react.useState(()=>initialFocusDate);
    const isInitialMount = _react.useRef(true);
    (0, _use_isomorphic_layout_effect.useIsomorphicLayoutEffect)(()=>{
        if (isInitialMount.current) {
            isInitialMount.current = false;
            return;
        }
        if (date != null) setViewDate(date);
    }, [
        date
    ]);
    const timezone = _react.useMemo(()=>defaultTimezone || (0, _utils.getTimezone)(viewDate, locale), [
        defaultTimezone,
        viewDate,
        locale
    ]);
    const todayTimezoneOffset = _react.useMemo(()=>defaultTimezone ? defaultTimezone.offset : (0, _utils.getTimezoneOffset)(today), [
        defaultTimezone,
        today
    ]);
    const dateInTimezone = _react.useMemo(()=>(0, _utils.toUTCDateTimeObject)((0, _utils.fromUTCToTimezone)(viewDate, timezone.offset)), [
        viewDate,
        timezone.offset
    ]);
    const focusedDateInTimezone = _react.useMemo(()=>{
        if (focusedDate == null) return undefined;
        return (0, _utils.toUTCDateTimeObject)((0, _utils.fromUTCToTimezone)(focusedDate, timezone.offset));
    }, [
        focusedDate,
        timezone.offset
    ]);
    const todayInTimezone = _react.useMemo(()=>(0, _utils.toUTCDateTimeObject)((0, _utils.fromUTCToTimezone)(today, todayTimezoneOffset)), [
        today,
        todayTimezoneOffset
    ]);
    const time = _react.useMemo(()=>(0, _utils.getTimeOfDay)(dateInTimezone), [
        dateInTimezone
    ]);
    const month = _react.useMemo(()=>(0, _utils.getMonth)(dateInTimezone), [
        dateInTimezone
    ]);
    const year = _react.useMemo(()=>(0, _utils.getYear)(dateInTimezone), [
        dateInTimezone
    ]);
    const dateToUtc = (0, _stable_function.useStableFunction)((dateInTz)=>{
        return toUtcDateString(dateInTz, (0, _utils.getTimeOfDay)(dateInTimezone), defaultTimezone);
    });
    const timeToUtc = (0, _stable_function.useStableFunction)((timeMs)=>{
        return toUtcDateString(dateInTimezone, timeMs, defaultTimezone);
    });
    const updateDate = (0, _stable_function.useStableFunction)((dateInTz, options)=>{
        const newUtcDate = dateToUtc(dateInTz);
        setViewDate(newUtcDate);
        if (options?.updateFocus) setFocusedDate(newUtcDate);
    });
    const updateTime = (0, _stable_function.useStableFunction)((timeMs)=>{
        setViewDate(timeToUtc(timeMs));
    });
    const updateFocusedDate = (0, _stable_function.useStableFunction)((dateInTz)=>{
        const newUtcDate = dateToUtc(dateInTz);
        setFocusedDate(newUtcDate);
        setViewDate(newUtcDate);
    });
    const unsetFocusedDate = (0, _stable_function.useStableFunction)(()=>{
        setFocusedDate(undefined);
    });
    return {
        timezone,
        dateInTimezone,
        focusedDateInTimezone,
        todayInTimezone,
        time,
        month,
        year,
        dateToUtc,
        timeToUtc,
        updateDate,
        updateTime,
        unsetFocusedDate,
        updateFocusedDate
    };
}
