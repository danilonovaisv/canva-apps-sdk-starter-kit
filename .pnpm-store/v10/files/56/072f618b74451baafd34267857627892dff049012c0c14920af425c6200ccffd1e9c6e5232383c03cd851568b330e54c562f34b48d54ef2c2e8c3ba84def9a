import { jsx as _jsx } from "react/jsx-runtime";
import classNames from 'classnames';
import 'react';
import { getSpaceValue } from '../../metrics/metrics';
import styles, { customProperties } from './divider.css';
export const Divider = ({ role, direction, borderWidth, inset = 'none', spacing = 'none' })=>{
    const insetStyle = inset !== 'none' ? {
        [customProperties.dividerInset]: getSpaceValue(inset)
    } : undefined;
    const spacingStyle = spacing !== 'none' ? {
        [customProperties.dividerSpacing]: getSpaceValue(spacing)
    } : undefined;
    const style = insetStyle || spacingStyle ? {
        ...insetStyle,
        ...spacingStyle
    } : undefined;
    return _jsx("hr", {
        role: role,
        "aria-orientation": role !== 'none' && direction === 'vertical' ? 'vertical' : undefined,
        className: classNames(styles.divider, {
            [styles.vertical]: direction === 'vertical',
            [styles.borderWidth200]: borderWidth === '200'
        }),
        style: style
    });
};
