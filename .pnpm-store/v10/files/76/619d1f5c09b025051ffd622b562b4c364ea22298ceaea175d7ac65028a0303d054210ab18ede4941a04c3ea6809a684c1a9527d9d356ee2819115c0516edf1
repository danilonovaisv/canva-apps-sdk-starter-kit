"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get InputWrapper () {
        return InputWrapper;
    },
    get useInputWrapperContext () {
        return useInputWrapperContext;
    }
});
const _jsxruntime = require("react/jsx-runtime");
const _preconditions = require('../../../../../base/preconditions');
const _compose_refs = require('../../../../../base/react/compose_refs');
const _classnames = _interop_require_default(require("classnames"));
const _react = _interop_require_wildcard(require("react"));
const _access_mode_state = require('../../../a11y/access_mode_state/access_mode_state');
const _pointer_input_state = require('../../../a11y/pointer_input_state/pointer_input_state');
const _base_inputcss = _interop_require_default(require("./base_input.css"));
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
const InputWrapper = (props)=>{
    const { blurOnEnterKeyDown, className, disabled, error, hoveredClassName: hoveredClassNameProp, activeClassName: activeClassNameProp, errorClassName: errorClassNameProp, disabledClassName: disabledClassNameProp, children, ref, testId } = props;
    const wrapperRef = _react.useRef(null);
    const { ref: inputRef, onClick: onWrapperClick } = useOnWrapperClick();
    const isKeyboardMode = (0, _access_mode_state.useIsKeyboardAccessMode)();
    const [isFocused, setFocused] = _react.useState(false);
    const isKeyboardFocused = isFocused && isKeyboardMode;
    const focusWrapper = _react.useCallback(()=>{
        wrapperRef.current?.focus();
    }, []);
    const hoverSupported = (0, _pointer_input_state.useIsHoveringInput)();
    const hoveredClassName = (0, _classnames.default)(hoveredClassNameProp, _base_inputcss.default.hoverSupported);
    const activeClassName = (0, _classnames.default)(activeClassNameProp, _base_inputcss.default.active);
    const errorClassName = (0, _classnames.default)(errorClassNameProp, _base_inputcss.default.error);
    const disabledClassName = (0, _classnames.default)(disabledClassNameProp, _base_inputcss.default.disabled);
    const wrapperClassName = (0, _classnames.default)(_base_inputcss.default.inputWrapper, disabled && disabledClassName || isFocused && activeClassName || error && errorClassName || hoverSupported && hoveredClassName, isKeyboardFocused && _base_inputcss.default.focusOutline, className);
    return (0, _jsxruntime.jsx)(InputWrapperContext, {
        value: {
            setFocused,
            inputRef,
            focusWrapper,
            blurOnEnterKeyDown,
            error,
            disabled
        },
        children: (0, _jsxruntime.jsx)("div", {
            ref: (0, _compose_refs.composeRefs)(wrapperRef, ref),
            tabIndex: blurOnEnterKeyDown ? -1 : undefined,
            onMouseUp: onWrapperClick,
            className: wrapperClassName,
            "data-testid": testId,
            children: children
        })
    });
};
const InputWrapperContext = _react.createContext(undefined);
function useInputWrapperContext() {
    const context = _react.useContext(InputWrapperContext);
    return _preconditions.Preconditions.checkExists(context, 'useInputWrapperContext must be used within a InputWrapper');
}
function useOnWrapperClick() {
    const ref = _react.useRef(null);
    const onClick = _react.useCallback((e)=>{
        let el = e.target;
        while(el && el !== e.currentTarget){
            if (isInteractive(el)) return;
            el = el.parentElement;
        }
        e.stopPropagation();
        e.preventDefault();
        ref.current?.focus();
    }, []);
    return {
        ref,
        onClick
    };
}
function isInteractive(el) {
    return el.tagName === 'A' || el.tagName === 'BUTTON' || el.tagName === 'INPUT' || el.tagName === 'TEXTAREA' || el.getAttribute('tabIndex') != null;
}
