import * as React from 'react';
import type { Theme } from '../../theme/theme';
export declare const iconSizes: readonly ["tiny", "small", "medium", "large"];
export type IconSize = (typeof iconSizes)[number];
export declare const iconTones: readonly ["primary", "secondary", "tertiary", "positive", "critical", "gradientBrand"];
export type IconTone = (typeof iconTones)[number];
export type Glyph = string | React.ComponentType;
export type GlyphMap = {
    [k in IconSize]?: Glyph;
};
export declare const storybookMetadata: WeakMap<Icon, {
    glyphs: GlyphMap;
    followDirection?: boolean;
}>;
export type IconProps = {
    className?: string
    style?: React.CSSProperties
    size?: IconSize
    tone?: IconTone
};
export type Icon = React.ComponentType<IconProps>;
type CreateIconOptions = {
    extraClassName?: string
    followDirection?: boolean
    defaultSize?: IconSize
    spinning?: boolean
    themed?: {
        [size in IconSize]?: Record<Theme, Glyph> | undefined;
    }
};
export declare function createIcon(
 glyphs: GlyphMap,
 { extraClassName, followDirection, defaultSize, spinning, themed, }?: CreateIconOptions
): Icon;
export declare function getGlyphAndClassForSize(size: IconSize, glyphs: GlyphMap): [Glyph, string];
export {};
