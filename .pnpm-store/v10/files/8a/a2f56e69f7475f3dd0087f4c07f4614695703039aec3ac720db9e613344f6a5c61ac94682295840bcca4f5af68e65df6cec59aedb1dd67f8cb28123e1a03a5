"use strict"
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "useResize", {
    enumerable: true,
    get: function() {
        return useResize;
    }
});
const _stable_function = require('../../../../../base/react/stable_function');
const _react = _interop_require_wildcard(require("react"));
const _multiline_inputcss = require("./multiline_input.css");
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
const DEFAULT_MIN_HEIGHT_PX = 40;
function useResize({ minHeightPx = DEFAULT_MIN_HEIGHT_PX } = {}) {
    const resizeContainerRef = _react.useRef(null);
    const resizeStartRef = _react.useRef(null);
    const onPointerDown = (0, _stable_function.useStableFunction)((e)=>{
        if (resizeContainerRef.current == null) return;
        e.preventDefault();
        e.target.setPointerCapture(e.pointerId);
        resizeStartRef.current = {
            y: e.clientY,
            height: resizeContainerRef.current.offsetHeight
        };
    });
    const onPointerMove = (0, _stable_function.useStableFunction)((e)=>{
        const start = resizeStartRef.current;
        if (start == null) return;
        const deltaY = e.clientY - start.y;
        const newHeight = Math.max(minHeightPx, start.height + deltaY);
        const resizeContainer = resizeContainerRef.current;
        if (resizeContainer != null) resizeContainer.style.setProperty(_multiline_inputcss.customProperties.internalWrapperHeight, `${newHeight}px`);
    });
    const onPointerUp = (0, _stable_function.useStableFunction)((e)=>{
        if (resizeStartRef.current != null) {
            e.target.releasePointerCapture(e.pointerId);
            resizeStartRef.current = null;
        }
    });
    const onTouchMove = (0, _stable_function.useStableFunction)((e)=>{
        e.preventDefault();
    });
    const resizeHandleRef = (0, _stable_function.useStableFunction)((element)=>{
        element.addEventListener('pointerdown', onPointerDown);
        element.addEventListener('pointermove', onPointerMove);
        element.addEventListener('pointerup', onPointerUp);
        element.addEventListener('pointercancel', onPointerUp);
        element.addEventListener('lostpointercapture', onPointerUp);
        element.addEventListener('touchmove', onTouchMove, {
            passive: false
        });
        return ()=>{
            element.removeEventListener('pointerdown', onPointerDown);
            element.removeEventListener('pointermove', onPointerMove);
            element.removeEventListener('pointerup', onPointerUp);
            element.removeEventListener('pointercancel', onPointerUp);
            element.removeEventListener('lostpointercapture', onPointerUp);
            element.removeEventListener('touchmove', onTouchMove);
        };
    });
    return {
        resizeContainerRef,
        resizeHandleRef
    };
}
