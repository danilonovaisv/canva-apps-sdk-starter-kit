"use strict"
Object.defineProperty(exports, "__esModule", {
    value: true
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get Duration () {
        return Duration;
    },
    get Flick () {
        return Flick;
    }
});
const _preconditions = require('../preconditions');
const _helpers = require("./private/helpers");
class Duration {
    static of(us) {
        return us === 0 ? Duration.zero : new Duration(us);
    }
    static from(input) {
        if (input instanceof Duration) return input;
        const ticks = asUs(input);
        return ticks === 0 ? Duration.zero : new Duration(ticks);
    }
    static fromSeconds(seconds) {
        return Duration.from({
            seconds
        });
    }
    static fromMilliseconds(milliseconds) {
        return Duration.from({
            milliseconds
        });
    }
    static fromMicroseconds(microseconds) {
        return Duration.from({
            microseconds
        });
    }
    toMicroseconds() {
        return this.us;
    }
    toMilliseconds() {
        return this.us / 1000;
    }
    toSeconds() {
        return this.us / _helpers.TicksPerSecond.MICROSECOND;
    }
    add(other) {
        return Duration.of(this.us + asUs(other));
    }
    subtract(other) {
        return Duration.of(this.us - asUs(other));
    }
    multiply(scalar) {
        (0, _helpers.assertFiniteScalar)(scalar);
        return Duration.of(Math.round(this.us * scalar));
    }
    divide(scalar) {
        _preconditions.Preconditions.checkArgument(scalar !== 0, 'Division by zero');
        (0, _helpers.assertFiniteScalar)(scalar);
        return Duration.of(Math.round(this.us / scalar));
    }
    negated() {
        return Duration.of(-this.us);
    }
    min(other) {
        return compareTimeLike(this, other) <= 0 ? this : Duration.of(asUs(other));
    }
    max(other) {
        return compareTimeLike(this, other) >= 0 ? this : Duration.of(asUs(other));
    }
    clamp(lo, hi) {
        if (compareTimeLike(lo, hi) > 0) throw new RangeError('min must be <= max');
        if (compareTimeLike(this, lo) < 0) return Duration.of(asUs(lo));
        if (compareTimeLike(this, hi) > 0) return Duration.of(asUs(hi));
        return this;
    }
    eq(other) {
        return compareTimeLike(this, other) === 0;
    }
    gt(other) {
        return compareTimeLike(this, other) > 0;
    }
    lt(other) {
        return compareTimeLike(this, other) < 0;
    }
    gte(other) {
        return compareTimeLike(this, other) >= 0;
    }
    lte(other) {
        return compareTimeLike(this, other) <= 0;
    }
    asDuration() {
        return this;
    }
    asFlick() {
        return Flick.from(this);
    }
    toString() {
        return (0, _helpers.formatDurationUs)(this.us);
    }
    constructor(us){
        _preconditions.Preconditions.checkArgument(Number.isSafeInteger(us), 'Tick count must be a safe integer, got {}', us);
        this.us = us;
    }
}
Duration.ticksPerSecond = _helpers.TicksPerSecond.MICROSECOND;
Duration.zero = new Duration(0);
class Flick {
    static of(flicks) {
        return flicks === 0 ? Flick.zero : new Flick(flicks);
    }
    static from(input) {
        if (input instanceof Flick) return input;
        const ticks = asFlicks(input);
        return ticks === 0 ? Flick.zero : new Flick(ticks);
    }
    toFlicks() {
        return this.flicks;
    }
    toMicroseconds() {
        return (0, _helpers.rescale)(this.flicks, _helpers.TicksPerSecond.FLICK, _helpers.TicksPerSecond.MICROSECOND);
    }
    toMilliseconds() {
        return this.flicks / (_helpers.TicksPerSecond.FLICK / 1000);
    }
    toSeconds() {
        return this.flicks / _helpers.TicksPerSecond.FLICK;
    }
    add(other) {
        return Flick.of(this.flicks + asFlicks(other));
    }
    subtract(other) {
        return Flick.of(this.flicks - asFlicks(other));
    }
    multiply(scalar) {
        (0, _helpers.assertFiniteScalar)(scalar);
        return Flick.of(Math.round(this.flicks * scalar));
    }
    divide(scalar) {
        _preconditions.Preconditions.checkArgument(scalar !== 0, 'Division by zero');
        (0, _helpers.assertFiniteScalar)(scalar);
        return Flick.of(Math.round(this.flicks / scalar));
    }
    negated() {
        return Flick.of(-this.flicks);
    }
    min(other) {
        return compareTimeLike(this, other) <= 0 ? this : Flick.of(asFlicks(other));
    }
    max(other) {
        return compareTimeLike(this, other) >= 0 ? this : Flick.of(asFlicks(other));
    }
    clamp(lo, hi) {
        if (compareTimeLike(lo, hi) > 0) throw new RangeError('min must be <= max');
        if (compareTimeLike(this, lo) < 0) return Flick.of(asFlicks(lo));
        if (compareTimeLike(this, hi) > 0) return Flick.of(asFlicks(hi));
        return this;
    }
    snap(ticksPerFrame, strategy = 'round') {
        if (!Number.isSafeInteger(ticksPerFrame) || ticksPerFrame <= 0) throw new RangeError(`ticksPerFrame must be a positive integer, got ${ticksPerFrame}`);
        const fn = strategy === 'floor' ? Math.floor : strategy === 'ceil' ? Math.ceil : Math.round;
        return Flick.of(fn(this.flicks / ticksPerFrame) * ticksPerFrame);
    }
    eq(other) {
        return compareTimeLike(this, other) === 0;
    }
    gt(other) {
        return compareTimeLike(this, other) > 0;
    }
    lt(other) {
        return compareTimeLike(this, other) < 0;
    }
    gte(other) {
        return compareTimeLike(this, other) >= 0;
    }
    lte(other) {
        return compareTimeLike(this, other) <= 0;
    }
    asDuration() {
        return Duration.from(this);
    }
    asFlick() {
        return this;
    }
    toString() {
        return `${this.flicks}@${_helpers.TicksPerSecond.FLICK}tps (${this.toSeconds()}s)`;
    }
    constructor(flicks){
        _preconditions.Preconditions.checkArgument(Number.isSafeInteger(flicks), 'Tick count must be a safe integer, got {}', flicks);
        this.flicks = flicks;
    }
}
Flick.ticksPerSecond = _helpers.TicksPerSecond.FLICK;
Flick.zero = new Flick(0);
function asUs(x) {
    if (x instanceof Duration) return x.toMicroseconds();
    if (x instanceof Flick) return (0, _helpers.rescale)(x.toFlicks(), _helpers.TicksPerSecond.FLICK, _helpers.TicksPerSecond.MICROSECOND);
    if ((0, _helpers.isFlickLiteral)(x)) return (0, _helpers.rescale)((0, _helpers.validateFlickLiteral)(x), _helpers.TicksPerSecond.FLICK, _helpers.TicksPerSecond.MICROSECOND);
    return (0, _helpers.unitsToTicks)(x, _helpers.TicksPerSecond.MICROSECOND);
}
function asFlicks(x) {
    if (x instanceof Duration) return (0, _helpers.rescale)(x.toMicroseconds(), _helpers.TicksPerSecond.MICROSECOND, _helpers.TicksPerSecond.FLICK);
    if (x instanceof Flick) return x.toFlicks();
    if ((0, _helpers.isFlickLiteral)(x)) return (0, _helpers.validateFlickLiteral)(x);
    return (0, _helpers.unitsToTicks)(x, _helpers.TicksPerSecond.FLICK);
}
function toBigIntPair(x) {
    if (x instanceof Duration) return [
        BigInt(x.toMicroseconds()),
        BigInt(_helpers.TicksPerSecond.MICROSECOND)
    ];
    if (x instanceof Flick) return [
        BigInt(x.toFlicks()),
        BigInt(_helpers.TicksPerSecond.FLICK)
    ];
    if ((0, _helpers.isFlickLiteral)(x)) return [
        BigInt((0, _helpers.validateFlickLiteral)(x)),
        BigInt(_helpers.TicksPerSecond.FLICK)
    ];
    return [
        (0, _helpers.unitsToBigIntNs)(x),
        BigInt(_helpers.NS_PER_SEC)
    ];
}
function sign(n) {
    if (n > 0n) return 1;
    if (n < 0n) return -1;
    return 0;
}
function compareTimeLike(a, b) {
    const aNonFinite = nonFiniteComponent(a);
    const bNonFinite = nonFiniteComponent(b);
    if (aNonFinite !== undefined || bNonFinite !== undefined) {
        const aValue = aNonFinite ?? 0;
        const bValue = bNonFinite ?? 0;
        if (Number.isNaN(aValue) || Number.isNaN(bValue)) return Number.NaN;
        if (aValue === bValue) return 0;
        return aValue < bValue ? -1 : 1;
    }
    const [aN, aD] = toBigIntPair(a);
    const [bN, bD] = toBigIntPair(b);
    if (aD === bD) return sign(aN - bN);
    return sign(aN * bD - bN * aD);
}
function nonFiniteComponent(x) {
    if (x instanceof Duration || x instanceof Flick) return undefined;
    let sum = 0;
    let hasNonFinite = false;
    for (const v of Object.values(x))if (v !== undefined) {
        sum += v;
        if (!Number.isFinite(v)) hasNonFinite = true;
    }
    return hasNonFinite ? sum : undefined;
}
