export type SurfaceTransitionOptions = {
    open: boolean
    transitionDurationMs: number
    disableAnimation?: boolean
    onOpenComplete?: () => void
    onCloseComplete?: () => void
};
export type SurfaceTransition = {
    animated: boolean
    rendering: boolean
    opening: boolean
    open: boolean
    closing: boolean
};
export declare function useSurfaceTransition(
 { open, transitionDurationMs, disableAnimation, onOpenComplete, onCloseComplete, }: SurfaceTransitionOptions
): SurfaceTransition;
