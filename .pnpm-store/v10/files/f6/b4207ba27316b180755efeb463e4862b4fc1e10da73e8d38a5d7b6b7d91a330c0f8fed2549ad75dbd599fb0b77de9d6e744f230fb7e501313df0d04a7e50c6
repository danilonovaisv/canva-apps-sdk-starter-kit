import type { TimeUnits } from './private/helpers';
import { TicksPerSecond } from './private/helpers';
export type Time = Duration | Flick;
export type TimeLike = Time | TimeUnits;
export type SnapStrategy = 'floor' | 'ceil' | 'round';
export declare class Duration {
    static readonly ticksPerSecond: TicksPerSecond;
    static readonly zero: Duration;
    private readonly us;
    private constructor();
    private static of;
    static from(input: TimeLike): Duration;
    static fromSeconds(seconds: number): Duration;
    static fromMilliseconds(milliseconds: number): Duration;
    static fromMicroseconds(microseconds: number): Duration;
    toMicroseconds(): number;
    toMilliseconds(): number;
    toSeconds(): number;
    add(other: TimeLike): Duration;
    subtract(other: TimeLike): Duration;
    multiply(scalar: number): Duration;
    divide(scalar: number): Duration;
    negated(): Duration;
    min(other: TimeLike): Duration;
    max(other: TimeLike): Duration;
    clamp(lo: TimeLike, hi: TimeLike): Duration;
    eq(other: TimeLike): boolean;
    gt(other: TimeLike): boolean;
    lt(other: TimeLike): boolean;
    gte(other: TimeLike): boolean;
    lte(other: TimeLike): boolean;
    asDuration(): Duration;
    asFlick(): Flick;
    toString(): string;
}
export declare class Flick {
    static readonly ticksPerSecond: TicksPerSecond;
    static readonly zero: Flick;
    private readonly flicks;
    private constructor();
    private static of;
    static from(input: TimeLike): Flick;
    toFlicks(): number;
    toMicroseconds(): number;
    toMilliseconds(): number;
    toSeconds(): number;
    add(other: TimeLike): Flick;
    subtract(other: TimeLike): Flick;
    multiply(scalar: number): Flick;
    divide(scalar: number): Flick;
    negated(): Flick;
    min(other: TimeLike): Flick;
    max(other: TimeLike): Flick;
    clamp(lo: TimeLike, hi: TimeLike): Flick;
    snap(ticksPerFrame: number, strategy?: SnapStrategy): Flick;
    eq(other: TimeLike): boolean;
    gt(other: TimeLike): boolean;
    lt(other: TimeLike): boolean;
    gte(other: TimeLike): boolean;
    lte(other: TimeLike): boolean;
    asDuration(): Duration;
    asFlick(): Flick;
    toString(): string;
}
